package com.venky.oracle.pkg10;

import java.util.function.Function;

/*
public class TripleThisC {
    public static void main(String[] args) {

        Function  tripler = x->{return (Integer)x*3;};
        TripleThisC.printValue(tripler,4);
    }

    private static <Integer>  void printValue(Function <Integer> f, int num) {
        System.out.println(f.apply(num));
    }
}
*/

//THis code is not goign to compile Function required two type parameters and one is Integer